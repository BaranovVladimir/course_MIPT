#include <stdio.h>

int main(int argc, char **argv)
{
    int a, i;
    int even = 0, odd = 0;

    scanf("%d", &a);

    for (i = 10; i <= a; i++)
    {
        if ;
        a /= 10;
    }

    printf("%d %d", even, odd);
    return 0;
}